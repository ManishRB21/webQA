[build]
  builder = "nixpacks"

[deploy]
  startCommand = "npm run build -w @webqa/shared && npm run build -w @webqa/engine && npm run build -w @webqa/server && npx playwright install chromium && npm run start:server"
  healthcheckPath = "/health"
  restartPolicyType = "on_failure"
